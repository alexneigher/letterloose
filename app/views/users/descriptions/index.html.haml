.row
  .col-md-12
    %h2
      All work by
      %span.text-letter-loose= @user.username
.row.row-flex.row-flex-wrap
  - @descriptions.each do |description|
    .col-md-4
      .panel.panel-default
        .panel-body{class: dom_id(description)}
          = render partial: 'shared/description', locals: {description: description}
